FROM golang:1.22-alpine AS builder

WORKDIR /build
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 go build -ldflags="-s -w" -o /ticketflow .

FROM alpine:3.19

RUN apk add --no-cache git bash

COPY --from=builder /ticketflow /ticketflow

ENTRYPOINT ["/ticketflow"]
